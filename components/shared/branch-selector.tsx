"use client";

// Branch selector removed — single branch (Business Bay) hardcoded everywhere.
// File kept to avoid import errors from any remaining references.
export function BranchSelector() { return null; }
export function LimitedBranchSelector(_: { allowedBranches: string[] }) { return null; }
